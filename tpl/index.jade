extends inc/layout

block variables
  - var rootdir = './'

block content
  include inc/top
  
  script(type='text/javascript').
    function loadercomplete() {
      Loader.load('./jst/index.js', 'Index');
      //Global.i()
    }
