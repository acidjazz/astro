
extends ../inc/layout

block variables
  - var rootdir = '../'

block content
  include ../inc/top

  .contact

    .banner
      .inner.off
        .copy
          .lcopy Solar System,
          .scopy Milky Way

    .block
      .inner
        .copy ASTRO is a creative agency on a mission to turn the worst day of the week into the best one.  Work with us?  Are you interested in collaborating? Grab your phone or keyboard and reach out to say hi.
 
    #map

  include ../inc/bottom

  script(src="https://maps.googleapis.com/maps/api/js").

  script(type='text/javascript').
    function loadercomplete() {
      Loader.load('../jst/contact.js', 'Contact');
      Global.i()
      accessToken = '#{data.mapbox.accessToken}';
      mapId = '#{data.mapbox.mapId}';
    }

