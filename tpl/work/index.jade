
extends ../inc/layout

block variables
  - var rootdir = '../'

block content
  include ../inc/top

  .projects

    // SUMMARY OF PROJECTS
    .summary.off
      .hero
        .copy1 change this
        .copy2 We’ve been creating the future for a while now.
      .thumbs
        each project in data.project
          .thumb.on(
            class='thumb_' + project.folder,
            data-project=project.folder,
            style='background-image: url("../img/project/' + project.folder + '/1440/' + project.cover + '");',
            data-filters=project.filters
          )
            .inner
              .copy
                .name #{project.name}
                .filters !{project.filters.join('&nbsp;&nbsp;/&nbsp;&nbsp;')}
      .clear
      .filters.float
        .inner
          .title CAPABILITIES
          .filtermenu
            each filter in data.filters
              if filter.name == 'all'
                .filter.on(class='filter_' + filter.name,data-copy=filter.copy) #{filter.name}
              else
                .filter(class='filter_' + filter.name,data-copy=filter.copy) #{filter.name}
          .clear


    // PROJECT PAGES
    - var project = data.project.threedr

    .project.project_threedr.off
      include _header
      .body
        include threedr
      include _credits

    - var project = data.project.versus

    .project.project_versus.off
      include _header
      .body
        include versus
      include _credits

    - var project = data.project.aos

    .project.project_aos.off
      include _header
      .body
        include aos
      include _credits

    - var project = data.project.goggle

    .project.project_goggle.off
      include _header
      .body
        include goggle
      include _credits


    - var project = data.project.fuelband

    .project.project_fuelband.off
      include _header
      .body
        include fuelband
      include _credits

    - var project = data.project.neff

    .project.project_neff.off
      include _header
      .body
        include neff
      include _credits

    - var project = data.project.mineralist

    .project.project_mineralist.off
      include _header
      .body
        include mineralist
      include _credits


    - var project = data.project.xbox

    .project.project_xbox.off
      include _header
      .body
        include xbox
      include _credits


    - var project = data.project.tamar

    .project.project_tamar.off
      include _header
      .body
        include tamar
      include _credits

    - var project = data.project.jinnitv

    .project.project_jinnitv.off
      include _header
      .body
        include jinnitv
      include _credits

    - var project = data.project.owlet

    .project.project_owlet.off
      include _header
      .body
        include owlet
      include _credits


    - var project = data.project.minus8

    .project.project_minus8.off
      include _header
      .body
        include minus8
      include _credits

    - var project = data.project.sol

    .project.project_sol.off
      include _header
      .body
        include sol
      include _credits

  include ../inc/bottom

  script(type='text/javascript').
    function loadercomplete() {
      Loader.load('../jst/work.js', 'Work');
      Global.i()
      projects = !{JSON.stringify(data.project)};
    }

